setup the environment for local agent development then start implementing this environment on addedin new features to the demo website project.

create a new branch/swith to the branch in git
each agent should have a unique name, so that when we stop one agent it dosent affect other agents. 
    - to resolve this the time stamp will be added to the agent name.
    - Store ID in registry
    - Stop/log/status use ID instead of name